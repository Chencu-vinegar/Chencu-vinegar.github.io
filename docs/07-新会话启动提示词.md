# 07 新会话启动提示词

> 用途：**换到新对话时，把下面的提示词整段复制过去**，新会话就不会丢失进度。
> 原则：本文件只是「入口」，事实以仓库文件为准 —— 新会话读完 `docs/00` 与 `git log` 即可对齐。

## 使用方法

| 场景 | 发哪条 |
| --- | --- |
| **任何新对话的第一条消息** | 提示词 A（接手项目） |
| 想补个人经历 / 作品素材 | 提示词 A → 然后 B |
| 想调整视觉、版式、配色 | 提示词 A → 然后 C |
| 想看反馈 / 加通知 | 提示词 A → 然后 D |
| 部署 / 验收线上数字分身（真实 AI） | 提示词 A → 然后 E |
| 线上打不开 / 样式错乱 / 提交失败 | 提示词 A → 然后 F |
| 不知道怎么描述需求 | 提示词 A → 直接用大白话说明想改什么 |

---

## 提示词 A · 接手项目（每次新对话都先发这条）

```text
项目：个人主页。工作目录 D:\个人主页

请先读 docs/00-项目交接与进度.md（新对话首读），再按需读 docs/05-技术方案.md；
不要全盘扫描仓库，也不要逐文件浏览。

当前状态（2026-09-24）：
- 已上线 GitHub Pages：https://chencu-vinegar.github.io/
  仓库 Chencu-vinegar.github.io（Public，用户名站点），发布源 master / (root)，git push 即自动发布。
- 本地已到 V4 标签；最新一批改动是「线上数字分身接真实大模型」。
- 纯静态 HTML/CSS/JS：无框架、无构建步骤、无 Node.js。不要引入 npm 或打包器。
- 数字分身：本地走 server.py 的 /api/chat；线上走 Supabase Edge Function（supabase/functions/chat/index.ts，
  密钥存 Supabase Secrets）。前端 main.js 有三层降级，最终回退 localReply 演示回复，页面绝不报错。

红线（务必遵守）：
1. scripts/main.js 必须保持 ES5：不用 let / const / 箭头函数 / 模板字符串。
2. 密钥只在服务端：本地 .env、线上 Supabase Secrets（Edge Function chat）。
   前端只用 Supabase publishable 公钥（scripts/config.js，里面还有分身函数地址 chatUrl，都不是密钥）。
   永不使用、永不写入 service_role 密钥；除非用户明确要求，不要把任何密钥写进仓库文件。
3. 不把 .env、.opencode/、opencode.jsonc、.deepworks/、uploads/ 提交进仓库。
4. 本站文案、配色与图形全部自制：不引入外部字体 / CDN / 图标库，
   不照搬他人站点的品牌名、原句文案、logo、插画或照片。
5. 动手前先看 git status / git log；改动后跑最小验证，不要覆盖已有工作。

环境坑（避免重踩）：
- PowerShell 控制台输出中文是乱码：判断文件内容请用 read 工具，不要靠 shell 回显。
- 没有 Node.js。Python 用 C:\Users\LENOVO\AppData\Local\Programs\Python\Python314\python.exe 或 py。
- 本地预览：py server.py 后访问 http://localhost:8000（启动/杀进程的正确方式见 docs/00 §6，
  不要用 Start-Process，会留僵尸进程）。
- 校验脚本：python .deepworks/tmp/check_layout.py（版式与选择器）、
  python .deepworks/tmp/check_deploy.py https://chencu-vinegar.github.io/（部署与连通性）。

请先做这四件事，然后用 5 行以内复述，再等我派活：
1. 读 docs/00
2. 跑 git log --oneline -5 与 git status --short
3. 跑 python .deepworks/tmp/check_deploy.py https://chencu-vinegar.github.io/（确认线上仍正常）
4. 告诉我「当前进度 / 线上状态 / 你认为的下一步」
```

---

## 提示词 B · 补内容（经历 / 作品 / 文案）

```text
（先发提示词 A）

我要补充主页内容，素材如下：
- 【经历】时间 + 机构/项目 + 角色 + 一句话成果，例如：
- 【作品】名称 + 简介 + 链接（如有）+ 封面图（如有）：
- 【其他】技能关键词、想改的措辞：

要求：
1. 放在 index.html 对应区块（经历=时间线，作品区块此前因无内容已移除，需要时按需加回）。
2. 保持现有「高对比色块版式」的标题与间距规范，样式沿用 styles/main.css 中已有的类，
   不要新引入字体或图标资源。
3. 不要编造我没提供的事实、数字或链接。
4. 改完跑 python .deepworks/tmp/check_layout.py，再用本地预览确认，最后告诉我改了哪些文件。
5. 我确认后再 git commit（并在需要上线时 git push）。
```

---

## 提示词 C · 调整视觉 / 版式 / 配色

```text
（先发提示词 A）

我想调整视觉：
- 目标（选填）：更简洁 / 更醒目 / 更专业 / 更活泼
- 具体位置（选填）：首屏 / 经历区块 / 联系卡片 / 页脚 / 移动端
- 参考感觉（选填）：一句话描述，或给我链接让我只借鉴版式语言

要求：
1. 主色保持蓝青：浅色 #2f6fed、深色 #5b8def、青色 #0fb5a5（要换请先跟我确认）。
2. 只动 styles/main.css 与 index.html 的结构类名，不要改 scripts/main.js 的交互契约
   （不要删改它依赖的元素 id / class / 表单 name）。
3. 必须同时验证浅色与深色、桌面（>1200px）与窄屏（≤ 800px）四种情况。
4. 改完跑 check_layout.py，并给我一个本地 http://localhost:8000 预览。
5. 不要引入外部字体 / CDN / 图标库；图形用 CSS 自绘。
```

---

## 提示词 D · 查看反馈 / 加新反馈通知

```text
（先发提示词 A）

背景：意见反馈存在 Supabase，项目 ref fudznxnxtoiaxcxcvfuq，表 public.feedback。
RLS 只放行匿名 INSERT，所以前端公钥读不到内容，只能在 Supabase 控制台（你登录后）查看。
控制台直达：https://supabase.com/dashboard/project/fudznxnxtoiaxcxcvfuq/editor

我想做（按需选其一）：
A) 只是查看/统计现有反馈 → 给我 SQL（总数、最近 N 条、按类型分组），并指导我删除测试数据。
B) 新反馈自动邮件通知我 → 用 Supabase Database Webhook + Edge Function（建议配合 Resend
   之类的邮件服务）实现，把密钥放在 Supabase 的 Secrets 里，绝不放前端。
C) 用 Edge Function + 只读 token 做一个简单的只读查看页 → 请先说明安全影响，我确认后再做。

要求：说明每一步我在浏览器里要点什么；涉及新密钥时告诉我放到哪里、以及如何避免提交进仓库。
```

---

## 提示词 E · 部署 / 验收线上数字分身（真实 AI）

> 方案已定并写好代码，**函数尚未部署**。只有部署 + 配 Secret 这一步需要用户操作（需登录 Supabase）。

```text
（先发提示词 A）

背景：线上数字分身已改为 Supabase Edge Function 承载密钥（supabase/functions/chat/index.ts），
前端三层降级与一键发布脚本 tools/deploy-chat-function.ps1 都已就绪，本地链路已实测。
现在要把它真正部署上线（Supabase 项目 ref fudznxnxtoiaxcxcvfuq，函数名必须是 chat）。

要求：
1. 先告诉我「网页粘贴部署」与「命令行部署（tools\deploy-chat-function.ps1）」两种方式各要点什么，
   并明确哪些步骤必须我本人登录/授权、哪些你可以代做。不要让我把任何密钥贴进聊天。
2. 我做完部署后，你负责验收：跑 python .deepworks/tmp/check_deploy.py https://chencu-vinegar.github.io/
   （它会 GET 函数健康探测并真实发一轮对话），必要时再核对 config.js 的 chatUrl。
3. 验收通过后再 git push；如未通过，按响应码定位：404 = 函数未部署/名字不对，
   401/403 = JWT 强校验没关，503 = Secrets 里缺 DEEPSEEK_API_KEY，502 = 上游 DeepSeek 报错。
4. 保留回退手段：config.js 的 chatUrl 置空即回到演示模式。
```

---

## 提示词 F · 线上故障排查

```text
（先发提示词 A）

线上出问题了，现象是：____（打不开 404 / 样式丢失 / 中文乱码 / 图片不显示 /
反馈提交失败 / 数字分身报错 / 改动推送后没生效）

请按这个顺序排查并直接动手修，不要只给我建议：
1. 先跑 python .deepworks/tmp/check_deploy.py https://chencu-vinegar.github.io/
   （它会报告首页与每个静态资源的状态码和字节数）。
2. 对照常见原因：
   - 404：Pages 是否启用、构建是否失败（仓库 Actions/Pages 页签）、分支是否选对 master/(root)
   - 样式/脚本丢失：资源是否用相对路径、文件名大小写是否一致
   - 中文乱码：index.html 是否有 <meta charset="UTF-8">、文件本身编码是否为 UTF-8
   - 反馈提交失败：scripts/config.js 的地址与公钥是否与 .env 指向同一项目、CORS 预检是否放行
   - 分身一直「演示模式」/回答报错：config.js 的 chatUrl 是否为空、Edge Function 是否部署
     （GET https://fudznxnxtoiaxcxcvfuq.supabase.co/functions/v1/chat 应返回 aiEnabled:true）、
     Secrets 里是否有 DEEPSEEK_API_KEY、函数是否关闭了 JWT 强校验
   - 推送没生效：是否真的 push 成功（git status / git log origin/master -1），再等 1–2 分钟或强刷缓存
3. 修完说明「根因 + 我改了什么 + 线上验证结果」。
```

---

## 附：最小上下文卡（只想贴一小段时用）

```text
个人主页项目 · D:\个人主页 · 已上线 https://chencu-vinegar.github.io/（GitHub Pages 用户名站点，
仓库 Chencu-vinegar.github.io，master 根目录，push 即发布）。纯静态 HTML/CSS/JS，无构建无 Node，
main.js 必须 ES5。反馈走 Supabase（ref fudznxnxtoiaxcxcvfuq，表 public.feedback，RLS 仅匿名 INSERT，
前端只用 publishable 公钥在 scripts/config.js），密钥只在服务端（本地 .env / 线上 Supabase Secrets）。
数字分身线上走 Supabase Edge Function（chat），失败自动回退演示回复。最新进度以 docs/00 为准；
先读 docs/00-项目交接与进度.md 再动手；中文请用 read 工具看，PowerShell 回显会乱码。
```
