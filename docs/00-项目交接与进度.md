# 00 项目交接与进度（Handoff）

> **给新对话/新会话的对接入口**：开始任何工作前，先读本文件即可快速对齐项目状态。
> 本文件是「当前进行到什么程度」的单一来源，请随进展同步更新。

## 1. 一句话项目概括

一个**静态个人主页**，用于展示个人品牌、作品、经历与联系方式，并含一个「我的数字分身」Demo 界面。

## 2. 已确认的关键决策（不可轻易推翻）

| 维度 | 决策 |
| --- | --- |
| 页面形态 | **单页滑动**（一个长页 + 锚点导航） |
| 视觉风格 | **极简专业**（留白、克制、灰白黑 + 单一强调色 `#2f6fed`） |
| 页面语言 | **中文** |
| 技术栈 | **纯静态 HTML / CSS / JS**（无框架、无构建） |
| 内容方式 | **先用占位内容演示**，后续再替换真实素材 |
| 深色模式 | 有（浅色默认 + 可切换深色，CSS 变量实现） |
| 数字分身 | 已搭好 **DeepSeek 接入框架**（Python 代理 + 前端调用 + 无密钥自动回退），待填入 API Key 启用真实 AI |

## 3. 内容 / 待办状态（勾选反映真实进度）

- [x] 需求方向确认（单页 / 极简专业 / 中文 / 纯静态 / 占位演示 / 深色可切换）
- [x] 占位 Demo 页面完成
- [x] 录入真实个人信息（姓名 / 身份 / 专业 / 邮箱 / 微信），并打 Git 标签 `V1`
- [x] 数字分身：接入框架完成（后端代理 + 前端调用 + 无密钥回退）
- [ ] 内容素材补齐（头像 / 更多经历 / 作品 —— 见 `04-内容清单.md`）
- [ ] 技术选型「部署平台」确定（纯静态已定，尚未选 GitHub Pages / Vercel / 其他）
- [ ] 为数字分身配置真实 API Key（`DEEPSEEK_API_KEY`）
- [ ] 部署上线

## 4. 文件结构（当前）

```text
个人主页/
├── README.md                 # 项目总览 + 文档索引
├── server.py                 # 零依赖本地服务：静态托管 + /api/chat 代理 DeepSeek
├── .env.example              # 环境变量示例（复制为 .env 并填密钥）
├── docs/
│   ├── 00-项目交接与进度.md   # ← 本文件：新对话首读
│   ├── 01-需求说明.md
│   ├── 02-页面结构与信息架构.md
│   ├── 03-设计规范.md
│   ├── 04-内容清单.md
│   ├── 05-技术方案.md
│   └── 06-开发计划与验收标准.md
├── index.html                # 单页主页（已录入真实信息 + 数字分身）
├── styles/main.css           # 全局样式（CSS 变量主题 + 分身 UI）
├── scripts/main.js           # 交互（主题/菜单/滚动/表单 + 分身 AI 调用与回退）
└── assets/images/            # 图片素材（avatar.jpg 已接入）
```

## 5. 源文件关键信息（新对话注意）

- **入口**：`index.html`（单页，所有区块集中在此文件）。
- **样式**：`styles/main.css` —— 主题用 CSS 变量（`:root` 浅色，`[data-theme="dark"]` 深色）。
- **交互**：`scripts/main.js` —— 纯原生 JS，无依赖；数字分身会调用后端 `/api/chat`，失败时回退到本地关键词回复。
- **数字分身后端**：`server.py`（Python 零依赖）——静态托管 + `/api/chat` 转发 DeepSeek；密钥从 `.env` 读取，前端不暴露。
- **区块顺序**：Hero → 关于我 → 经历 → **数字分身** → 联系我 → 页脚（作品区块暂未展示，已移除）。
- **HTML 锚点**：`#about` `#experience` `#avatar` `#contact`。
- **内容状态**：已替换为真实信息（姓名 陈渠梁、院校专业、邮箱、微信）；头像已接入真实图片 `assets/images/avatar.jpg`（替换该文件即可更换，首屏与数字分身共用），作品与更多经历待补。

## 6. 环境与 Git

- **Git**：已安装（`master` 分支），署名 `chencu080922 <chencu080922@qq.com>`。
- **远程仓库**：**尚未关联**（用户明确暂不连接 GitHub/Gitee）。
- **每次改动后的约定**：改完可以 `git add -A && git commit -m "说明"` 保存版本。
- **版本标签**：已创建 `V1`（录入个人信息的首版），指向提交 `685a0a5`。
- `.gitignore` 已排除 `.opencode/` 运行时与依赖、`.deepworks/`、日志、`.env`、`__pycache__/` 等。

## 7. 下一步（给新对话的建议起点）

1. 若用户要继续**补内容**：让用户按 `docs/04-内容清单.md` 提供素材，我替换 `index.html` 中的占位内容。
2. 若用户要**调整视觉/布局**：修改 `styles/main.css` 与 `index.html` 对应区块。
3. 若用户要**启用数字分身真实 AI**：把 `.env.example` 复制为 `.env`，填入 `DEEPSEEK_API_KEY`，运行 `py server.py` 后访问 `http://localhost:8000`。
4. 若用户要**部署**：先确定平台（GitHub Pages / Vercel / 其他），再配置与上线。

## 8. 变更记录

- 2026-09-04：建立项目文档体系 + 静态单页 Demo + 数字分身 Demo + 初始化 Git 环境与首次提交。
- 2026-09-10：录入真实个人信息并打标签 `V1`；数字分身接入 DeepSeek 框架（`server.py` 代理 + 前端调用 + 无密钥自动回退），新增 `.env.example`。
- 2026-09-17：接入头像图片 `assets/images/avatar.jpg`，首屏头像与数字分身形象改为真实图片，并加「无图片自动兜底」逻辑（首屏「陈」字 / 分身「渠」字）。
