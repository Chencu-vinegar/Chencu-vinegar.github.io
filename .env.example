# ============================================================
#  数字分身 · 环境变量示例
# ============================================================
# 使用方法：
#   1. 把本文件复制一份并重命名为 .env（不要提交到 Git！）
#   2. 在 .env 中填入你的真实 DeepSeek API Key
#   3. 运行：py server.py
#
# 获取密钥：https://platform.deepseek.com/  → API Keys
# ============================================================

# 必填：DeepSeek API 密钥（形如 sk-xxxxxxxx）
DEEPSEEK_API_KEY=

# ---------- 以下均为可选，留空则使用默认值 ----------

# 模型名称（默认 deepseek-chat；也可用 deepseek-reasoner）
# DEEPSEEK_MODEL=deepseek-chat

# 接口地址（一般无需修改）
# DEEPSEEK_API_BASE=https://api.deepseek.com/chat/completions

# 单次回复的最大 token 数（默认 800）
# DEEPSEEK_MAX_TOKENS=800

# 随机性 0~2，越大越发散（默认 0.7）
# DEEPSEEK_TEMPERATURE=0.7

# 请求超时秒数（默认 60）
# DEEPSEEK_TIMEOUT=60

# 本地服务监听地址与端口（默认 127.0.0.1:8000）
# HOST=127.0.0.1
# PORT=8000
